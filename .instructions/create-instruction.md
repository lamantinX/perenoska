---
description: Воркфлоу создания новой инструкции — проверка области, выбор типа (standard/validation/create/modify), frontmatter, секции, регистрация.
standard: .instructions/standard-instruction.md
standard-version: v1.3
index: .instructions/README.md
---

# Воркфлоу создания

Рабочая версия стандарта: 1.3

Пошаговый процесс создания новой инструкции.

**Полезные ссылки:**
- [Инструкции](./README.md)

**Связанные документы:**

| Тип | Документ |
|-----|----------|
| Стандарт | [standard-instruction.md](./standard-instruction.md) |
| Валидация | [validation-instruction.md](./validation-instruction.md) |
| Создание | Этот документ |
| Модификация | [modify-instruction.md](./modify-instruction.md) |

## Оглавление

- [Принципы](#принципы)
- [Шаги](#шаги)
  - [Шаг 1: Определить необходимость](#шаг-1-определить-необходимость)
  - [Шаг 2: Подготовка](#шаг-2-подготовка)
  - [Шаг 3: Проверить существующие инструкции](#шаг-3-проверить-существующие-инструкции)
  - [Шаг 4: Создать файл](#шаг-4-создать-файл)
  - [Шаг 5: Заполнить содержание](#шаг-5-заполнить-содержание)
  - [Шаг 5.1: Семантический анализ (для standard-*)](#шаг-51-семантический-анализ-для-standard-)
  - [Шаг 5.2: Согласование изменений с пользователем (для standard-*)](#шаг-52-согласование-изменений-с-пользователем-для-standard-)
  - [Шаг 6: Создать скрипты (если нужно)](#шаг-6-создать-скрипты-если-нужно)
  - [Шаг 7: Обновить README области](#шаг-7-обновить-readme-области)
  - [Шаг 8: Валидация](#шаг-8-валидация)
  - [Шаг 9: Ревью и создание скилла](#шаг-9-ревью-и-создание-скилла)
  - [Шаг 9.1: Создать/обновить rule](#шаг-91-создатьобновить-rule)
  - [Шаг 10: Отчёт о проделанной работе](#шаг-10-отчёт-о-проделанной-работе)
- [Чек-лист](#чек-лист)
- [Примеры](#примеры)
- [Скрипты](#скрипты)
- [Скиллы](#скиллы)

---

## Принципы

> **Создавать инструкцию только при повторяемости.** Паттерн должен встречаться > 2 раз.

> **Не дублировать SSOT.** Если есть источник истины — ссылаться, не копировать.

> **Инструкции = КАК делать, README = ЧТО есть.** Описание структуры папок — это README, не инструкция.

> **Скрипты автоматизируют шаги.** Если шаг требует поиска/фильтрации/трансформации — создать скрипт.

> **Шаблоны — из примеров SSOT.** При создании файлов использовать шаблоны из секции "Примеры". Запрещено придумывать свой формат.

> **Строгий порядок: standard → validation → create → modify.** Параллельное создание запрещено. Пропуск шагов невозможен. Изменение порядка невозможно.

> **Режим Agent (опционально).** Инструкции типов validation, create, modify МОГУТ создаваться агентом под контролем основного LLM. Standard всегда создаётся интерактивно с пользователем.

**Создавать инструкцию когда:**

| Критерий | Порог |
|----------|-------|
| Повторяемость паттерна | > 2 случаев |
| Сложность правил | > 3 правил |
| Область применения | > 1 файла/компонента |

**НЕ создавать инструкцию когда:**

- Описание структуры папок (→ README)
- Разовое решение (→ комментарий)
- Очевидные правила (→ здравый смысл)
- Уже есть похожая инструкция (→ расширить существующую)

---

## Шаги

### Шаг 1: Определить необходимость

```
Вопросы:
1. Какую проблему решает инструкция?
2. К каким файлам/компонентам применяется?
3. Есть ли связанные инструкции?
```

### Шаг 2: Подготовка

**Выбрать тип:**

| Тип | Префикс | Когда |
|-----|---------|-------|
| standard | `standard-` | Формат и правила объекта |
| create | `create-` | Процесс создания |
| modify | `modify-` | Процесс изменения |
| validation | `validation-` | Процесс проверки |

**Выбрать расположение:**

```
{область}/.instructions/{prefix}-{object}.md
```

| Область | Путь |
|---------|------|
| Общие инструкции | `/.instructions/` |
| Скиллы | `/.claude/.instructions/skills/` |
| Структура | `/.structure/.instructions/` |
| Спецификации | `/specs/.instructions/` |

**Проверить существование папки области:**

> **Если папка `{область}/` не существует:**

1. Предложить пользователю (AskUserQuestion): "Папка `{область}/` не существует. Создать?"
2. При подтверждении — вызвать `/structure-create {область}`
3. `/structure-create` автоматически создаст:
   - `{область}/README.md`
   - `{область}/.instructions/README.md`
4. После создания — продолжить с текущим шагом

**Выбрать имя:**
- Kebab-case: `error-handling.md`
- Описательное: `api-versioning.md`
- Короткое: `naming.md`

**Проверить порядок создания:**

> **ОБЯЗАТЕЛЬНО** для типов validation, create, modify — проверить наличие предыдущих типов в цепочке.

| Создаём | Требуется сначала |
|---------|-------------------|
| `validation-{object}.md` | `standard-{object}.md` |
| `create-{object}.md` | `standard-{object}.md` + `validation-{object}.md` |
| `modify-{object}.md` | все предыдущие |

**Если предыдущий тип отсутствует:**

> **БЛОКИРУЮЩЕЕ ТРЕБОВАНИЕ:** Создание невозможно без предыдущих типов в цепочке.

1. Сообщить пользователю: "Для создания `{type}-{object}.md` сначала нужно создать `{missing-type}-{object}.md`"
2. Автоматически вызвать `/instruction-create {missing-type}-{object}` для недостающего типа
3. После создания недостающего типа — продолжить с текущей инструкцией

### Шаг 3: Проверить существующие инструкции

> **ОБЯЗАТЕЛЬНО** — убедиться, что нет похожей инструкции.

**Получить список инструкций:**
```bash
python .instructions/.scripts/list-instructions.py
```

**Вывод:** список всех инструкций с описаниями — передать LLM для анализа.

**LLM анализирует:**
1. Есть ли инструкция на эту тему?
2. Есть ли похожая инструкция, которую можно расширить?
3. Есть ли SSOT, на который нужно ссылаться?

**Результаты:**

| Результат | Действие |
|-----------|----------|
| Инструкция уже есть | Спросить пользователя: "Инструкция {name} уже существует. Расширить её?" |
| Похожая инструкция | Спросить пользователя: "Найдена похожая инструкция {name}. Расширить или создать новую?" |
| Есть SSOT | Ссылаться на SSOT, не дублировать |
| Ничего не найдено | Продолжить создание |

### Шаг 4: Создать файл

**Автоматически:**
```bash
python .instructions/.scripts/create-instruction-file.py {name} {type} --area {path}
```

**Примеры:**
```bash
# Создать standard-инструкцию в корневой папке
python .instructions/.scripts/create-instruction-file.py api-versioning standard

# Создать validation-инструкцию в области structure
python .instructions/.scripts/create-instruction-file.py links validation --area .structure/.instructions

# С кастомным описанием
python .instructions/.scripts/create-instruction-file.py error-handling standard --description "Стандарт обработки ошибок в API"
```

**Вручную (если нужно):**
1. Создать файл по шаблону из [standard-instruction.md § 6. Шаблоны](./standard-instruction.md#6-шаблоны)
2. Заполнить frontmatter — SSOT: [standard-frontmatter.md](/.structure/.instructions/standard-frontmatter.md)

### Шаг 5: Заполнить содержание

> **Если тип ≠ standard:** прочитать `standard-{object}.md` области как SSOT перед заполнением содержания.

Содержание зависит от типа инструкции — см. [standard-instruction.md](./standard-instruction.md):
- standard → [§ 2](./standard-instruction.md#2-инструкции-типа-standard)
- create → [§ 3](./standard-instruction.md#3-инструкции-типа-create)
- modify → [§ 4](./standard-instruction.md#4-инструкции-типа-modify)
- validation → [§ 5](./standard-instruction.md#5-инструкции-типа-validation)

### Шаг 5.1: Семантический анализ (для standard-*)

> **ОБЯЗАТЕЛЬНО для типа standard** — после заполнения содержания запустить семантический анализ.

**Условие:** Тип инструкции = `standard-*`

1. Определить корневую папку инструкций — ближайшая папка `.instructions/` в пути:
   - `.github/.instructions/development/standard-development.md` → корень = `.github/.instructions/`
   - `.instructions/standard-script.md` → корень = `.instructions/`

2. Найти все `standard-*.md` в корне и подпапках:
   ```
   Glob: {корень}/**/standard-*.md
   ```

3. Для каждого найденного файла прочитать поле `description` из frontmatter

4. Исключить анализируемый файл из списка

5. Сформировать контекст зон:
   ```
   Соседние стандарты и их зоны ответственности:
   - standard-commit.md: Conventional Commits, типы, scope, правила оформления
   - standard-sync.md: Триггеры синхронизации, процесс, конфликты
   - standard-branching.md: Именование и создание веток
   - ...
   ```

6. Запустить агента meta-reviewer с контекстом зон:
   ```
   Task tool:
     subagent_type: meta-reviewer
     prompt: |
       Проанализируй файл: {путь к инструкции}

       Зона ответственности этого документа: {description из frontmatter}

       Соседние стандарты и их зоны ответственности (НЕ предлагай добавлять контент из этих зон):
       {список из шага 5}

       Ограничение: анализируй ТОЛЬКО в рамках зоны ответственности этого документа.
       Если тема покрыта соседним стандартом — допустима только SSOT-ссылка, не дублирование контента.
   ```

**Результат анализа:**
- Агент meta-reviewer создаёт драфт с анализом в `.claude/drafts/`
- Драфт содержит список проблем с приоритетами (P1, P2, P3)
- Проблемы ограничены зоной ответственности документа — зональные вторжения исключены

**Цель:** Стандарты — фундамент для validation/create/modify. Ошибки в стандарте каскадно влияют на все зависимые документы.

### Шаг 5.2: Согласование изменений с пользователем (для standard-*)

> **ОБЯЗАТЕЛЬНО для типа standard** — после анализа meta-reviewer согласовать изменения с пользователем.

**Действие:**

1. Показать пользователю путь к драфту с анализом
2. Попросить пользователя просмотреть анализ
3. Для каждой проблемы спросить решение через `AskUserQuestion` или свободный ввод:
   - "да" — применить рекомендацию
   - "нет" — пропустить
   - Альтернативное решение

**Формат запроса:**

```
Анализ meta-reviewer завершён.
Драфт: {путь к драфту}

Пожалуйста, просмотрите анализ и укажите, какие изменения вносить.
Формат ответа: номер проблемы + решение (да/нет/альтернатива)

Пример:
1.1 да
1.2 нет
2.1 да, но в другом формате: ...
```

**После получения ответов:**
- Применить одобренные изменения к инструкции
- Пропустить отклонённые
- Валидировать результат

### Шаг 6: Создать скрипты (если нужно)

> **Для типов create, modify, validation:** если шаги инструкции требуют автоматизации — создать скрипты.

**Когда создавать скрипт:**
- Шаг требует поиска файлов по критерию
- Шаг требует парсинга/трансформации данных
- Шаг требует валидации

**Процесс:**

#### 6.1. Получить список всех скриптов

```bash
python .instructions/.scripts/parse-docstrings.py
```

**Вывод:** список всех скриптов с описаниями — передать LLM для анализа.

#### 6.2. LLM анализирует

1. Есть ли скрипт с аналогичным функционалом?
2. Есть ли похожий скрипт, который можно доработать?
3. В какой директории находится потенциально подходящий скрипт?

#### 6.3. Если найден похожий скрипт

**Прочитать:**
- Код скрипта
- Связанную инструкцию (из секции "Скрипты")
- Понять контекст использования

**Анализ:**
- Можно ли переиспользовать как есть?
- Нужна ли доработка?
- Нужен ли новый скрипт в своей директории?

#### 6.4. Определить действие

> **Если НЕ найден похожий скрипт:** автоматически создать новый — `/script-create`

> **Если найден похожий скрипт:** спросить пользователя через `AskUserQuestion`:
> 1. Переиспользовать существующий скрипт `{name}` — он уже делает нужное
> 2. Доработать существующий скрипт `{name}` — добавить функционал
> 3. Создать новый скрипт в `{область}/.instructions/.scripts/`

#### 6.5. Выполнить действие

| Ситуация | Действие |
|----------|----------|
| Нет похожего скрипта | Автоматически вызвать `/script-create` |
| Переиспользовать | Добавить ссылку в секцию "Скрипты" |
| Доработать | Вызвать `/script-modify {путь}` |
| Создать новый | Вызвать `/script-create` |

**Скиллы для работы со скриптами:**

| Скилл | Назначение |
|-------|------------|
| [/script-create](/.claude/skills/script-create/SKILL.md) | Создание нового скрипта |
| [/script-modify](/.claude/skills/script-modify/SKILL.md) | Обновление, рефакторинг, удаление |
| [/script-validate](/.claude/skills/script-validate/SKILL.md) | Валидация формата скрипта |

### Шаг 7: Обновить README области

> **ОБЯЗАТЕЛЬНО перед валидацией** — добавить инструкцию в README папки `.instructions/`.

Формат README — см. [standard-readme.md § 3](/.structure/.instructions/standard-readme.md#3-readme-папок-инструкций).

**Что обновить:**
- Добавить строку в таблицу инструкций
- Обновить оглавление (если есть)

### Шаг 8: Валидация

```bash
python .instructions/.scripts/validate-instruction.py {path}
```

### Шаг 9: Ревью и создание скилла

> **ОБЯЗАТЕЛЬНО** использовать `AskUserQuestion` tool с кликабельными опциями.

**Вопрос:** "Содержимое инструкции корректно?"

**Опции:**
1. Да, всё корректно
2. Нет, нужны правки

**При выборе "Да, всё корректно":**

Если инструкция типа `create-`, `modify-` или `validation-`:
- **АВТОМАТИЧЕСКИ** вызвать `/skill-create` для этой инструкции
- Перейти к Шагу 10

**При выборе "Нет, нужны правки":**

1. Спросить: "Что нужно исправить?" (свободный ввод)
2. Внести изменения в инструкцию согласно ответу пользователя
3. **Повторить Шаг 9** (цикл до выбора "Да, всё корректно")

### Шаг 9.1: Создать/обновить rule

> **Условие:** Создан хотя бы 1 скилл для области.

> **ОБЯЗАТЕЛЬНО** — если для области создан скилл, нужен rule для автоматической загрузки контекста.

**Действие:** Вызвать `/rule-create` для создания или обновления rule области.

**Контекст для передачи:**
- Область инструкций (agents, api, hooks, database и т.д.)
- Список скиллов области (даже если пока только один)

**Вызов:**
```
/rule-create [область] --skills "/[область]-validate"
```

> **Примечание:** Скилл `/rule-create` сам определяет:
> - Целевой rule (существующий или новый)
> - Paths для области
> - Формат содержимого

> **Важно:** Rule создаётся даже для одного скилла — он обеспечивает автоматическую загрузку контекста при работе с файлами области.

### Шаг 10: Отчёт о проделанной работе

```
## 📋 Отчёт о создании инструкции

✅ **Создана инструкция:** `{путь к инструкции}`
🏷️ **Тип:** {standard/create/modify/validation}
📝 **Описание:** {description из frontmatter}

### Чек-лист

#### Подготовка
- [x/] Определена область применения
- [x/] Выбран тип
- [x/] Проверен порядок создания
- [x/] Выбрано расположение
- [x/] Проверено существование папки области
- [x/] Выбрано имя файла

#### Создание
- [x/] Проверено существование похожей инструкции
- [x/] Проверены SSOT
- [x/] Создан файл по шаблону
- [x/] Заполнен frontmatter
- [x/] Написано содержание
- [x/] Добавлена таблица "Связанные документы"
- [x/] Собраны зоны соседних стандартов (для standard)
- [x/] Семантический анализ meta-reviewer (для standard)
- [x/] Изменения согласованы с пользователем (для standard)

#### Скрипты (для create/modify/validation)
- [x/] Определены шаги для автоматизации
- [x/] Проверены существующие скрипты
- [x/] Созданы новые скрипты
- [x/] Скрипты добавлены в секцию "Скрипты"

#### Проверка
- [x/] README области обновлён
- [x/] Валидация пройдена
- [x/] Ревью завершено
- [x/] Скилл создан (для create/modify/validation)
- [x/] Rule создан/обновлён (для create/modify/validation)

Где [x] = выполнено, [ ] = пропущено/не применимо.
```

---

## Чек-лист

### Подготовка
- [ ] Определена область применения
- [ ] Выбран тип (standard/create/modify/validation)
- [ ] Проверен порядок создания (предыдущие типы существуют)
- [ ] Выбрано расположение
- [ ] Проверено существование папки области (если нет — `/structure-create`)
- [ ] Выбрано имя файла

### Создание
- [ ] Проверено существование похожей инструкции
- [ ] Проверены SSOT (не дублируем)
- [ ] Создан файл по шаблону
- [ ] Заполнен frontmatter
- [ ] Прочитан стандарт области (для типов ≠ standard)
- [ ] Написано содержание
- [ ] Добавлена таблица "Связанные документы"
- [ ] Собраны зоны ответственности соседних стандартов (для standard)
- [ ] Семантический анализ meta-reviewer пройден с контекстом зон (для standard)
- [ ] Изменения согласованы с пользователем (для standard)

### Скрипты (для create/modify/validation)
- [ ] Определены шаги, требующие автоматизации
- [ ] Проверены существующие скрипты
- [ ] Созданы новые скрипты (если нужно)
- [ ] Скрипты добавлены в секцию "Скрипты"

### Проверка
- [ ] README области обновлён
- [ ] Все ссылки валидны
- [ ] Валидация пройдена
- [ ] Ревью завершено
- [ ] Скилл создан (для create/modify/validation)
- [ ] Rule создан/обновлён (для create/modify/validation)
- [ ] Отчёт выведен пользователю

---

## Примеры

### Создание standard-инструкции

```bash
# 1. Определить: нужна инструкция для API-версионирования
# 2. Тип: standard, путь: /src/.instructions/standard-api-versioning.md
# 3. Создать файл по шаблону standard из standard-instruction.md § 6
# 4. Заполнить секции: правила версионирования, примеры
# 5. Скрипты: не нужны (тип standard)
# 6. Обновить /src/.instructions/README.md
# 7. Валидация
python .instructions/.scripts/validate-instruction.py src/.instructions/standard-api-versioning.md
```

### Создание validation-инструкции со скриптом

```bash
# 1. Определить: нужна валидация для API
# 2. Тип: validation, путь: /src/.instructions/validation-api.md
# 3. Создать файл по шаблону validation из standard-instruction.md § 6
# 4. Заполнить: Когда валидировать, Шаги, Типичные ошибки

# 5. Скрипты: шаг "Проверить все эндпоинты" требует автоматизации
python .instructions/.scripts/parse-docstrings.py --search "эндпоинт"
# Не найден → создать validate-api.py
# Добавить в секцию "Скрипты"

# 6. Обновить /src/.instructions/README.md
# 7. Валидация
python .instructions/.scripts/validate-instruction.py src/.instructions/validation-api.md
```

---

## Скрипты

| Скрипт | Назначение | Инструкция |
|--------|------------|------------|
| [list-instructions.py](./.scripts/list-instructions.py) | Список всех инструкций с описаниями | Этот документ |
| [create-instruction-file.py](./.scripts/create-instruction-file.py) | Создание файла инструкции по шаблону | Этот документ |
| [validate-instruction.py](./.scripts/validate-instruction.py) | Валидация формата инструкции | [validation-instruction.md](./validation-instruction.md) |
| [parse-docstrings.py](./.scripts/parse-docstrings.py) | Поиск скриптов по описанию | [create-script.md](./create-script.md) |

---

## Скиллы

| Скилл | Назначение | Инструкция |
|-------|------------|------------|
| [/instruction-create](/.claude/skills/instruction-create/SKILL.md) | Создание инструкции | Этот документ |
| [/rule-create](/.claude/skills/rule-create/SKILL.md) | Создание/обновление rule | [create-rule.md](/.claude/.instructions/rules/create-rule.md) |
| [/rule-modify](/.claude/skills/rule-modify/SKILL.md) | Обновление rule | [modify-rule.md](/.claude/.instructions/rules/modify-rule.md) |
