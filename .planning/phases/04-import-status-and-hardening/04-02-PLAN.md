# Plan 04-02

## Goal

Включить новый client contract в transfer lifecycle без смены текущей job architecture.

## Files

- `tests/test_transfer_preview.py`
- `app/services/transfer.py` if required by regressions

## Steps

1. Добавить integration tests для launch/sync с `yandex_market` как target.
2. Убедиться, что `submitted`, `processing`, `completed`, `failed` корректно проходят через текущий `TransferService`.
3. Зафиксировать failure surfacing через `error_message`.
