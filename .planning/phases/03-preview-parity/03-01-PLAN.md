---
phase: 03-preview-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas.py
  - app/services/transfer.py
  - app/services/mapping.py
  - tests/test_transfer_preview.py
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-03
  - PREV-04
  - PREV-05
  - PREV-06
  - MAP-04
must_haves:
  truths:
    - "Preview works for all four directions involving Yandex Market through the shared transfer preview API"
    - "Each preview item exposes hybrid readiness semantics instead of only a global ready_to_import bit"
    - "Saved category and controlled-value mappings involving Yandex Market are automatically reused in later preview runs"
  artifacts:
    - path: "app/services/transfer.py"
      provides: "Symmetric preview orchestration and item readiness model"
      contains: "preview("
    - path: "app/schemas.py"
      provides: "Preview response schema support for hybrid item readiness"
      contains: "TransferPreviewItem"
    - path: "tests/test_transfer_preview.py"
      provides: "Regression coverage for four Yandex preview directions and mapping reuse"
      contains: "yandex_market"
  key_links:
    - from: "app/services/transfer.py"
      to: "app/services/mapping.py"
      via: "preview readiness derives from payload-building and mapping results"
      pattern: "build_import_payload"
    - from: "app/services/transfer.py"
      to: "tests/test_transfer_preview.py"
      via: "preview contract is locked by API-level regression tests"
      pattern: "/api/v1/transfers/preview"
user_setup: []
---

<objective>
Upgrade backend preview semantics so Yandex Market participates symmetrically in preview generation and mapping reuse.

Purpose: Phase 3 starts by making the preview API itself trustworthy before media/UI layers are refined.
Output: A symmetric preview backend with explicit per-item readiness and regression tests for all four Yandex directions.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-parity/03-CONTEXT.md
@.planning/phases/03-preview-parity/03-RESEARCH.md
@app/schemas.py
@app/services/transfer.py
@app/services/mapping.py
@tests/test_transfer_preview.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit hybrid readiness semantics to preview items and preserve strict response readiness</name>
  <files>app/schemas.py, app/services/transfer.py, tests/test_transfer_preview.py</files>
  <read_first>app/schemas.py, app/services/transfer.py, app/api/routes/transfers.py, tests/test_transfer_preview.py, .planning/phases/03-preview-parity/03-CONTEXT.md, .planning/phases/03-preview-parity/03-RESEARCH.md</read_first>
  <action>Extend the preview schema so each `TransferPreviewItem` can expose a deterministic readiness state for the hybrid model: fully ready, needs mapping, or blocked. Implement this in `app/services/transfer.py` using concrete conditions from the existing preview pipeline: unresolved target category, unresolved controlled-value or category issues, missing required attributes, missing critical fields, and warning-only cases. Keep response-level `ready_to_import` strict so it is `true` only when every item is fully ready for import. Do not create a separate preview route or marketplace-specific schema branch.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k "preview and yandex"</verify>
  <acceptance_criteria>
    - `app/schemas.py` contains a readiness field or enum on `TransferPreviewItem`
    - `app/services/transfer.py` contains logic that assigns more than one item readiness state
    - `app/services/transfer.py` still contains `ready_to_import=`
    - `tests/test_transfer_preview.py` contains at least one assertion for a non-ready Yandex preview item state
  </acceptance_criteria>
  <done>Preview items expose hybrid readiness without weakening the global import gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add symmetric preview regression coverage for all Yandex directions and saved-mapping reuse</name>
  <files>tests/test_transfer_preview.py, app/services/transfer.py, app/services/mapping.py</files>
  <read_first>tests/test_transfer_preview.py, app/services/transfer.py, app/services/mapping.py, app/clients/wb.py, app/clients/ozon.py, app/clients/yandex_market.py, .planning/phases/03-preview-parity/03-CONTEXT.md</read_first>
  <action>Add or extend API-level preview tests so all four directions involving Yandex Market are explicitly covered: `yandex_market -> wb`, `yandex_market -> ozon`, `wb -> yandex_market`, and `ozon -> yandex_market`. Include assertions that preview surfaces missing required fields and category or mapping issues when Yandex is the source or target, and add at least one rerun case proving that a previously saved Yandex-related mapping is automatically reused on the next preview request. Update backend preview or mapping logic only as needed to make those tests pass through the existing transfer service path.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k yandex</verify>
  <acceptance_criteria>
    - `tests/test_transfer_preview.py` contains preview cases for `target_marketplace\": \"yandex_market\"`
    - `tests/test_transfer_preview.py` contains preview cases for `source_marketplace\": \"yandex_market\"`
    - `tests/test_transfer_preview.py` contains an assertion involving saved mapping reuse on a later preview run
    - `app/services/transfer.py` still contains saved mapping loading via `list_mappings`
  </acceptance_criteria>
  <done>The preview API has explicit regression coverage for symmetric Yandex flows and mapping reuse.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_transfer_preview.py -k yandex` passes
- [ ] All four Yandex preview directions are covered by tests or direct acceptance criteria
- [ ] Per-item readiness semantics exist without weakening `ready_to_import`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The preview backend is symmetric and actionable for Yandex flows
</success_criteria>

<output>
After completion, create `.planning/phases/03-preview-parity/03-01-SUMMARY.md`
</output>
