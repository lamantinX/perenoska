---
phase: 03-preview-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - app/static/app.js
  - app/static/index.html
  - app/static/app.css
  - tests/test_transfer_preview.py
autonomous: true
requirements:
  - UI-03
  - UI-04
  - PREV-06
  - PREV-07
must_haves:
  truths:
    - "Users can run preview flows involving Yandex Market through the built-in UI without falling back to manual API calls"
    - "The UI shows actionable preview feedback for category issues, required fields, dictionary gaps, and media warnings"
    - "The current preview-driven mapping and rerun flow remains usable for Yandex in both source and target roles"
  artifacts:
    - path: "app/static/app.js"
      provides: "Preview rendering with hybrid readiness and actionable issue surfacing"
      contains: "renderPreview"
    - path: "app/static/index.html"
      provides: "Existing preview surface and modal shell reused for richer Yandex feedback"
      contains: "previewCardModal"
    - path: "app/static/app.css"
      provides: "Visual support for new readiness and warning states"
      contains: "preview"
  key_links:
    - from: "app/static/app.js"
      to: "/api/v1/transfers/preview"
      via: "UI consumes the upgraded preview contract"
      pattern: "transfers/preview"
    - from: "app/static/app.js"
      to: "/api/v1/mappings/categories"
      via: "actionable preview feedback leads into category mapping flow"
      pattern: "mappings/categories"
user_setup: []
---

<objective>
Expose Phase 3 preview parity through the current built-in UI with actionable, not merely decorative, feedback.

Purpose: The backend parity work is not sufficient unless users can understand and act on preview results in the existing SPA.
Output: Preview cards and modal show hybrid readiness, media warnings, and mapping issues coherently for Yandex-related flows.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-parity/03-CONTEXT.md
@app/static/index.html
@app/static/app.js
@app/static/app.css
@app/api/routes/transfers.py
@app/api/routes/mappings.py
@tests/test_transfer_preview.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render hybrid readiness and actionable issue summaries in the current preview list</name>
  <files>app/static/app.js, app/static/index.html, app/static/app.css</files>
  <read_first>app/static/app.js, app/static/index.html, app/static/app.css, app/schemas.py, app/services/transfer.py, .planning/phases/03-preview-parity/03-CONTEXT.md</read_first>
  <action>Update the existing preview list rendering in `app/static/app.js` so each card can show the hybrid item state coming from the backend or derived from backend fields: ready, needs mapping, or blocked. Add concise but actionable summaries on the card for category mismatch, missing required attributes, controlled-value gaps, and media warnings, without dumping full raw diagnostics into the list. Reuse the current preview panel and card structure in `app/static/index.html`; only make minimal markup changes needed to support the new status chips or summaries. Adjust `app/static/app.css` so the new states and warning summaries remain readable on desktop and mobile, especially when category or media messages are long.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k yandex</verify>
  <acceptance_criteria>
    - `app/static/app.js` contains rendering logic for at least three preview readiness states or equivalent labels
    - `app/static/app.js` contains issue-summary handling for media warnings or mapping issues
    - `app/static/index.html` still contains the existing preview surface rather than a new page
    - `app/static/app.css` contains style support for preview status or warning presentation
  </acceptance_criteria>
  <done>The preview list becomes actionable and reflects hybrid readiness for Yandex-related transfers.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the preview-card and mapping-modal flow without adding a new UI shell</name>
  <files>app/static/app.js, app/static/index.html, app/static/app.css, tests/test_transfer_preview.py</files>
  <read_first>app/static/app.js, app/static/index.html, app/static/app.css, app/api/routes/mappings.py, app/services/transfer.py, tests/test_transfer_preview.py</read_first>
  <action>Update the preview-card modal and existing mapping-modal flow so Yandex-related preview issues are easy to inspect and act on inside the current SPA. Ensure the preview-card modal can display media-related warnings, mapped attributes, and missing-field details for Yandex target scenarios without assuming only WB or Ozon semantics. Preserve the current rerun-preview behavior after saving mappings and keep the UI anchored to the existing buttons and modals. Extend backend-facing or route-level tests only where needed to lock the preview contract that this UI depends on.</action>
  <verify>python -m pytest</verify>
  <acceptance_criteria>
    - `app/static/app.js` still contains the existing preview-card modal flow via `openPreviewCardModal`
    - `app/static/app.js` contains Yandex-aware issue rendering in the preview-card or mapping modal path
    - `app/static/app.js` still triggers preview reruns after saving mappings
    - `tests/test_transfer_preview.py` remains green with the upgraded preview contract
  </acceptance_criteria>
  <done>The current UI flow can inspect and act on Yandex preview issues without a parallel interface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest` succeeds
- [ ] Built-in preview UI reflects hybrid readiness and actionable issue summaries
- [ ] Mapping save and rerun flow remains coherent for Yandex-related previews
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The existing SPA can drive and interpret Yandex preview parity
</success_criteria>

<output>
After completion, create `.planning/phases/03-preview-parity/03-03-SUMMARY.md`
</output>
