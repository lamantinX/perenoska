---
phase: 03-preview-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/services/transfer.py
  - app/services/mapping.py
  - tests/test_transfer_preview.py
autonomous: true
requirements:
  - PREV-05
  - PREV-06
  - PREV-07
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-05
must_haves:
  truths:
    - "Preview preserves product images for supported Yandex-related directions instead of silently dropping them"
    - "Preview emits actionable media warnings when image completeness is insufficient for the chosen direction"
    - "Media and mapping issues influence hybrid item readiness without collapsing every warning into a hard block"
  artifacts:
    - path: "app/services/transfer.py"
      provides: "Media preservation and warning synthesis in preview"
      contains: "warnings"
    - path: "app/services/mapping.py"
      provides: "Target payload building keeps media attached where direction supports it"
      contains: "images"
    - path: "tests/test_transfer_preview.py"
      provides: "Media preservation and warning regressions for Yandex preview directions"
      contains: "images"
  key_links:
    - from: "app/services/transfer.py"
      to: "app/services/mapping.py"
      via: "preview warnings and payload completeness are derived from built target payloads"
      pattern: "payload"
    - from: "tests/test_transfer_preview.py"
      to: "app/services/transfer.py"
      via: "tests lock media warning semantics and image preservation"
      pattern: "warnings"
user_setup: []
---

<objective>
Make media a first-class part of preview parity for Yandex-related transfers.

Purpose: Symmetric preview is not operationally useful if images disappear silently or media insufficiency is invisible.
Output: Media-preserving preview payloads with actionable warnings and regression coverage.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-parity/03-CONTEXT.md
@.planning/phases/03-preview-parity/03-RESEARCH.md
@app/services/transfer.py
@app/services/mapping.py
@app/clients/wb.py
@app/clients/ozon.py
@app/clients/yandex_market.py
@tests/test_transfer_preview.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve images across Yandex preview directions and warn when media is insufficient</name>
  <files>tests/test_transfer_preview.py, app/services/mapping.py, app/services/transfer.py</files>
  <read_first>tests/test_transfer_preview.py, app/services/mapping.py, app/services/transfer.py, app/clients/wb.py, app/clients/ozon.py, app/clients/yandex_market.py, .planning/phases/03-preview-parity/03-RESEARCH.md</read_first>
  <action>Start with tests in `tests/test_transfer_preview.py` that assert media preservation for `yandex_market -> wb`, `yandex_market -> ozon`, `wb -> yandex_market`, and `ozon -> yandex_market` wherever the normalized source product already contains images. Then update payload-building and preview logic so target preview payloads retain image data in the marketplace-appropriate draft shape instead of dropping it during transformation. Add explicit preview warnings for at least these concrete cases: source product has no images, target payload ends up with no images although source had them, or the image collection is empty after normalization. Keep these warnings actionable and compatible with the hybrid readiness model from Plan 01.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k media</verify>
  <acceptance_criteria>
    - `tests/test_transfer_preview.py` contains assertions involving `images`
    - `tests/test_transfer_preview.py` contains at least one media-warning assertion for a Yandex-related direction
    - `app/services/transfer.py` contains warning text or logic for missing or incomplete media
    - `app/services/mapping.py` contains image-preservation logic in target payload construction
  </acceptance_criteria>
  <done>Preview payloads keep media and report actionable media insufficiency instead of failing silently.</done>
</task>

<task type="auto">
  <name>Task 2: Keep media warnings actionable without turning every warning into a hard blocker</name>
  <files>app/services/transfer.py, tests/test_transfer_preview.py</files>
  <read_first>app/services/transfer.py, tests/test_transfer_preview.py, app/schemas.py, .planning/phases/03-preview-parity/03-CONTEXT.md</read_first>
  <action>Refine preview readiness logic so media insufficiency participates in the hybrid status model but does not automatically mark every item as blocked. Express concrete behavior in tests: warning-only media degradation should still allow a draft preview item, while truly incomplete target payloads that cannot satisfy minimal target preview shape should land in a non-ready state with an actionable warning. Reuse the same preview response shape from Plan 01 rather than inventing a separate media-status contract.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k "warning or media"</verify>
  <acceptance_criteria>
    - `app/services/transfer.py` contains logic that distinguishes warning-only media cases from stronger non-ready cases
    - `tests/test_transfer_preview.py` contains at least one assertion where warnings exist but the item still has a draft preview payload
    - `tests/test_transfer_preview.py` contains at least one assertion where media affects item readiness or non-ready status
  </acceptance_criteria>
  <done>The media policy matches the locked decision: validated and actionable, but not universally strict-gated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_transfer_preview.py -k media` passes
- [ ] Preview payloads preserve images in Yandex-related directions where the source includes them
- [ ] Media insufficiency appears as actionable warnings, not silent omission
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Media handling in preview is explicit and testable for Yandex flows
</success_criteria>

<output>
After completion, create `.planning/phases/03-preview-parity/03-02-SUMMARY.md`
</output>
