---
phase: 02-category-and-mapping-model
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - app/static/app.js
  - app/static/index.html
  - app/static/app.css
  - tests/test_transfer_preview.py
autonomous: true
requirements:
  - MAP-02
  - MAP-03
must_haves:
  truths:
    - "Users can save Yandex Market category mappings through the current preview-driven UI flow"
    - "Users can save Yandex Market controlled-value mappings through the current UI flow where the backend supports them"
    - "Rerunning preview after saving a Yandex-related mapping reuses the saved rule instead of presenting the same unresolved issue"
  artifacts:
    - path: "app/static/app.js"
      provides: "Current-flow UI wiring for Yandex category and controlled-value mapping saves"
      contains: "mappings/categories"
    - path: "app/static/index.html"
      provides: "UI surface for the existing mapping dialogs"
      contains: "categoryMapping"
    - path: "tests/test_transfer_preview.py"
      provides: "End-to-end preview/mapping reuse coverage"
      contains: "yandex_market"
  key_links:
    - from: "app/static/app.js"
      to: "/api/v1/mappings/categories"
      via: "manual category mapping save"
      pattern: "mappings/categories"
    - from: "app/static/app.js"
      to: "/api/v1/mappings/dictionary"
      via: "manual controlled-value mapping save"
      pattern: "mappings/dictionary"
user_setup: []
---

<objective>
Expose the new Yandex mapping capabilities through the current built-in UI flow.

Purpose: Near-parity MVP requires users to resolve and save Yandex-related mappings in the existing interface, not only through backend APIs.
Output: Current mapping dialogs and preview rerun behavior work for Yandex Market scenarios.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-and-mapping-model/02-CONTEXT.md
@app/static/app.js
@app/static/index.html
@app/static/app.css
@app/api/routes/mappings.py
@tests/test_transfer_preview.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Yandex Market into the existing category-mapping UI flow</name>
  <files>app/static/app.js, app/static/index.html, app/static/app.css</files>
  <read_first>app/static/app.js, app/static/index.html, app/static/app.css, app/api/routes/mappings.py, app/services/transfer.py, .planning/phases/02-category-and-mapping-model/02-CONTEXT.md</read_first>
  <action>Update the current category-mapping modal flow in `app/static/app.js` so category issues involving `yandex_market` can be displayed, selected, and saved through `POST /api/v1/mappings/categories` without assuming only Ozon or WB target contexts. Preserve the existing modal and button structure in `app/static/index.html`; do not add a separate mappings screen. Where labels currently assume Ozon or WB-specific fields, switch to marketplace-neutral rendering that can show Yandex category path context from `target_context`. Touch `app/static/app.css` only if needed to keep long Yandex labels readable in the existing dialog layout.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k category</verify>
  <acceptance_criteria>
    - `app/static/app.js` contains `mappings/categories`
    - `app/static/app.js` contains `yandex_market`
    - `app/static/app.js` contains rendering logic for category issue options with path or label context
    - `app/static/app.js` does not hardcode category-save behavior to Ozon-only target fields
  </acceptance_criteria>
  <done>The current category-mapping UI can save mappings involving Yandex Market.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Yandex controlled-value mapping reuse into the existing preview-driven UI</name>
  <files>app/static/app.js, app/static/index.html, app/static/app.css, tests/test_transfer_preview.py</files>
  <read_first>app/static/app.js, app/api/routes/mappings.py, app/services/transfer.py, tests/test_transfer_preview.py, tests/test_dictionary_mappings.py</read_first>
  <action>Update `app/static/app.js` so the existing dictionary or brand mapping interactions can submit controlled-value mappings for Yandex Market scenarios through `POST /api/v1/mappings/dictionary` when the preview response exposes a Yandex-supported issue. Keep the UI within the current flow: issue rendering, option selection, save action, and preview rerun. Add or extend backend-facing tests in `tests/test_transfer_preview.py` so a preview scenario involving Yandex Market demonstrates that saving the mapping removes or reduces the repeated issue on the next preview run.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k mapping</verify>
  <acceptance_criteria>
    - `app/static/app.js` contains `mappings/dictionary`
    - `app/static/app.js` contains Yandex-aware controlled-value or brand-mapping handling
    - `tests/test_transfer_preview.py` contains a rerun-preview assertion after saving a Yandex mapping
    - `app/static/app.js` still contains the existing preview refresh path after mapping save
  </acceptance_criteria>
  <done>The current UI can drive save-and-rerun behavior for Yandex-related controlled-value mappings.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest` succeeds
- [ ] Current mapping dialogs work with Yandex-related issues without introducing a new UI surface
- [ ] Preview reruns consume saved Yandex mappings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Yandex Market is usable in the existing mapping UI flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-category-and-mapping-model/02-03-SUMMARY.md`
</output>
