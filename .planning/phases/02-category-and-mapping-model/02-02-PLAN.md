---
phase: 02-category-and-mapping-model
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/services/mapping.py
  - app/services/transfer.py
  - app/api/routes/mappings.py
  - app/db.py
  - tests/test_dictionary_mappings.py
  - tests/test_transfer_preview.py
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
must_haves:
  truths:
    - "Auto-match can propose Yandex Market category mappings using the current generalized mapping architecture"
    - "Manual category mappings involving Yandex Market are saved and reused through the existing mappings store"
    - "Manual controlled-value mappings involving Yandex Market can be validated, saved, and reused without Ozon-only hard blocks"
  artifacts:
    - path: "app/api/routes/mappings.py"
      provides: "Marketplace-aware category and controlled-value mapping save routes"
      contains: "yandex_market"
    - path: "app/services/transfer.py"
      provides: "Saved-mapping reuse during preview preparation"
      contains: "saved_category_mappings"
    - path: "tests/test_transfer_preview.py"
      provides: "Preview-path reuse tests for category and controlled-value mappings"
      contains: "yandex_market"
    - path: "tests/test_dictionary_mappings.py"
      provides: "Generalized persistence regression coverage"
      contains: "mapping"
  key_links:
    - from: "app/api/routes/mappings.py"
      to: "app/db.py"
      via: "save_mapping persists generalized category and controlled-value rules"
      pattern: "save_mapping"
    - from: "app/services/transfer.py"
      to: "app/api/routes/mappings.py"
      via: "preview reuse consumes mappings saved through the API"
      pattern: "list_mappings"
user_setup: []
---

<objective>
Generalize mapping persistence and reuse so Yandex Market participates in category and controlled-value flows.

Purpose: Phase 2 is not complete with metadata alone; preview and later import work depend on reusable saved mappings rather than one-off category guesses.
Output: Generalized backend mapping flow for Yandex Market plus regression tests for save and reuse behavior.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-and-mapping-model/02-CONTEXT.md
@.planning/phases/02-category-and-mapping-model/02-RESEARCH.md
@app/services/mapping.py
@app/services/transfer.py
@app/api/routes/mappings.py
@app/db.py
@tests/test_dictionary_mappings.py
@tests/test_transfer_preview.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend category auto-match and saved category mapping semantics for Yandex Market</name>
  <files>app/services/mapping.py, app/services/transfer.py, app/api/routes/mappings.py, tests/test_transfer_preview.py</files>
  <read_first>app/services/mapping.py, app/services/transfer.py, app/api/routes/mappings.py, app/clients/yandex_market.py, tests/test_transfer_preview.py, .planning/phases/02-category-and-mapping-model/02-CONTEXT.md</read_first>
  <action>Update category issue generation and save handling so Yandex Market categories use marketplace-native category IDs as the stable key while preserving long path or label context in `source_context` and `target_context`. In `app/api/routes/mappings.py`, validate Yandex target category mappings against real Yandex categories instead of Ozon- or WB-specific assumptions. In `app/services/transfer.py`, ensure `_resolve_saved_category_mapping()` and `_category_issue_options()` can reuse saved mappings involving `Marketplace.YANDEX_MARKET` and expose options with diagnostic path context. Keep the generalized mapping type as `category`, not a Yandex-specific table or route.</action>
  <verify>python -m pytest tests/test_transfer_preview.py -k category</verify>
  <acceptance_criteria>
    - `app/api/routes/mappings.py` contains `Marketplace.YANDEX_MARKET`
    - `app/services/transfer.py` contains logic handling `yandex_market` in category issue or saved-category resolution paths
    - `tests/test_transfer_preview.py` contains a saved category mapping scenario involving `yandex_market`
    - `app/services/transfer.py` still contains `_resolve_saved_category_mapping`
  </acceptance_criteria>
  <done>Category auto-match and saved category mappings can now include Yandex Market without breaking existing preview flows.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize controlled-value mapping saves beyond the current Ozon-only path</name>
  <files>app/api/routes/mappings.py, app/services/mapping.py, app/db.py, tests/test_dictionary_mappings.py, tests/test_transfer_preview.py</files>
  <read_first>app/api/routes/mappings.py, app/services/mapping.py, app/db.py, app/clients/ozon.py, app/clients/yandex_market.py, tests/test_dictionary_mappings.py, tests/test_transfer_preview.py</read_first>
  <action>Refactor `save_dictionary_mappings()` in `app/api/routes/mappings.py` so it becomes a marketplace-aware controlled-value mapping path instead of rejecting every non-Ozon target. Preserve the existing Ozon behavior, but add the Yandex Market branch that validates controlled values using normalized Yandex parameter metadata and available option IDs from `get_category_attributes()` rather than Ozon `get_dictionary_values()`. Continue storing these mappings in the generalized `mappings` table with `mapping_type` values such as `dictionary_brand` or another existing controlled-value convention; do not add a new Yandex-only storage table. In `app/services/mapping.py`, consume saved Yandex controlled-value mappings when building mapped attribute values, including preserved target option IDs and labels.</action>
  <verify>python -m pytest tests/test_dictionary_mappings.py tests/test_transfer_preview.py -k dictionary</verify>
  <acceptance_criteria>
    - `app/api/routes/mappings.py` no longer contains a hard rejection that manual dictionary mappings are supported only for Ozon
    - `app/api/routes/mappings.py` contains `Marketplace.YANDEX_MARKET`
    - `app/services/mapping.py` contains saved-mapping reuse logic that is not Ozon-only for controlled values
    - `tests/test_transfer_preview.py` contains a controlled-value mapping save or reuse scenario involving `yandex_market`
  </acceptance_criteria>
  <done>Controlled-value mappings involving Yandex Market can be validated, persisted, and reused by the backend.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_dictionary_mappings.py tests/test_transfer_preview.py -k mapping` passes
- [ ] Category mappings involving Yandex Market are stored in the generalized mappings table
- [ ] Controlled-value mappings involving Yandex Market can be reused on later preview runs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend mapping persistence and reuse are generalized to include Yandex Market
</success_criteria>

<output>
After completion, create `.planning/phases/02-category-and-mapping-model/02-02-SUMMARY.md`
</output>
