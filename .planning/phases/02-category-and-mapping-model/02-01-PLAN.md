---
phase: 02-category-and-mapping-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/clients/yandex_market.py
  - app/api/routes/catalog.py
  - tests/test_yandex_market_client.py
autonomous: true
requirements:
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "User can load Yandex Market categories as target metadata through the existing catalog API"
    - "User can load Yandex Market category parameters in a normalized form suitable for later preview and import planning"
    - "Yandex Market metadata preserves import-leaning context such as controlled values, constraints, and units where available"
  artifacts:
    - path: "app/clients/yandex_market.py"
      provides: "Yandex Market category and parameter metadata support"
      contains: "get_category_attributes"
    - path: "app/api/routes/catalog.py"
      provides: "Catalog attribute API exposes Yandex Market metadata through the shared route shape"
      contains: "categories/{category_id}/attributes"
    - path: "tests/test_yandex_market_client.py"
      provides: "Metadata normalization coverage for categories and attributes"
      contains: "attributes"
  key_links:
    - from: "app/api/routes/catalog.py"
      to: "app/clients/yandex_market.py"
      via: "catalog service attribute route delegates Yandex Market target metadata"
      pattern: "get_category_attributes"
user_setup: []
---

<objective>
Implement the Yandex Market target metadata layer needed by later preview and import work.

Purpose: Phase 2 starts by replacing the current Phase 1 stub with real category-parameter metadata so later plans are built on stable target semantics rather than placeholders.
Output: Yandex Market category attributes available through the shared catalog API with import-leaning normalization and tests.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-category-and-mapping-model/02-CONTEXT.md
@.planning/phases/02-category-and-mapping-model/02-RESEARCH.md
@app/clients/base.py
@app/clients/yandex_market.py
@app/api/routes/catalog.py
@tests/test_yandex_market_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Yandex Market category-parameter retrieval and normalization</name>
  <files>app/clients/yandex_market.py</files>
  <read_first>app/clients/base.py, app/clients/yandex_market.py, app/clients/ozon.py, app/schemas.py, .planning/phases/02-category-and-mapping-model/02-CONTEXT.md, .planning/phases/02-category-and-mapping-model/02-RESEARCH.md</read_first>
  <action>Implement `async def get_category_attributes(self, credentials, category_id, *, required_only=False)` in `app/clients/yandex_market.py` using the official `POST /v2/category/{categoryId}/parameters` endpoint and pass `businessId` when the API supports cabinet-specific parameter behavior. Normalize the response into `CategoryAttribute` objects with concrete values for `id`, `name`, `required`, and `dictionary_values`. Preserve import-leaning raw metadata inside `raw`, including whether the parameter allows custom values, whether it is multivalue, any available unit/default-unit data, constraints, value type hints, and any Yandex value IDs returned by the API. If `required_only=True`, filter the normalized list before returning it rather than mutating raw response shape.</action>
  <verify>python -m pytest tests/test_yandex_market_client.py -k attributes</verify>
  <acceptance_criteria>
    - `app/clients/yandex_market.py` contains `async def get_category_attributes`
    - `app/clients/yandex_market.py` contains `/v2/category/`
    - `app/clients/yandex_market.py` contains `required_only`
    - `app/clients/yandex_market.py` contains `dictionary_values`
    - `app/clients/yandex_market.py` contains `allowCustomValues` or normalized handling of the same field
  </acceptance_criteria>
  <done>The Yandex client can return normalized target parameter metadata rich enough for later preview and import phases.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests proving category and attribute metadata flows through the shared catalog surface</name>
  <files>tests/test_yandex_market_client.py</files>
  <read_first>tests/test_yandex_market_client.py, tests/test_ozon_client.py, app/clients/yandex_market.py, app/api/routes/catalog.py</read_first>
  <action>Extend `tests/test_yandex_market_client.py` with stubbed responses for `POST /v2/category/{categoryId}/parameters` and assertions that the normalized result includes required flags, controlled values, raw constraint hints, and optional filtering by `required_only=True`. Add or extend a route-level test that exercises `GET /api/v1/catalog/categories/{category_id}/attributes?marketplace=yandex_market` and verifies the response contains Yandex parameter IDs, names, and preserved metadata needed by later mapping logic.</action>
  <verify>python -m pytest tests/test_yandex_market_client.py</verify>
  <acceptance_criteria>
    - `tests/test_yandex_market_client.py` contains `test_get_category_attributes`
    - `tests/test_yandex_market_client.py` contains `required_only=True`
    - `tests/test_yandex_market_client.py` contains `/api/v1/catalog/categories/`
    - `tests/test_yandex_market_client.py` contains `marketplace=yandex_market`
  </acceptance_criteria>
  <done>The repo has focused regression coverage for Yandex category and parameter metadata retrieval.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_yandex_market_client.py` passes
- [ ] Yandex Market category-attribute metadata is available through the shared catalog route
- [ ] Metadata normalization includes import-leaning context instead of required-only placeholders
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Yandex Market target metadata is available for later mapping and preview phases
</success_criteria>

<output>
After completion, create `.planning/phases/02-category-and-mapping-model/02-01-SUMMARY.md`
</output>
