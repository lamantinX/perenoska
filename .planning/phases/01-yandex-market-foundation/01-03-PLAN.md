---
phase: 01-yandex-market-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - app/static/index.html
  - app/static/app.js
  - app/static/app.css
  - tests/test_auth_and_connections.py
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "User can see and submit a Yandex Market connection form in the built-in UI"
    - "User can select Yandex Market in marketplace selectors"
    - "User can use the source catalog workflow with Yandex Market from the existing UI"
  artifacts:
    - path: "app/static/index.html"
      provides: "Yandex Market form and selector markup"
      contains: "yandex_market"
    - path: "app/static/app.js"
      provides: "Yandex Market UI wiring for connections, selectors, and source catalog"
      contains: "yandex_market"
    - path: "app/static/app.css"
      provides: "Styling support for the added UI surface"
    - path: "tests/test_auth_and_connections.py"
      provides: "Backend regression coverage for UI-consumed contracts"
      contains: "yandex_market"
  key_links:
    - from: "app/static/app.js"
      to: "/api/v1/connections/yandex_market"
      via: "connection form submission"
      pattern: "connections/yandex_market"
    - from: "app/static/app.js"
      to: "/api/v1/catalog/products"
      via: "source catalog load using marketplace=yandex_market"
      pattern: "catalog/products\\?marketplace="
user_setup: []
---

<objective>
Expose Yandex Market in the embedded SPA for connection management, selectors, and source catalog use.

Purpose: Phase 1 is not complete until users can see and use the new marketplace in the existing product surface.
Output: UI form, selector support, and source catalog behavior for Yandex Market.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@app/static/index.html
@app/static/app.js
@app/static/app.css
@app/api/routes/connections.py
@app/api/routes/catalog.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Yandex Market connection and selector UI surface</name>
  <files>app/static/index.html, app/static/app.js, app/static/app.css</files>
  <read_first>app/static/index.html, app/static/app.js, app/static/app.css, app/schemas.py, app/api/routes/connections.py</read_first>
  <action>Update `app/static/index.html` to add a Yandex Market connection form with inputs named `token`, `business_id`, and `campaign_id`, plus badge and masked-field placeholders matching the existing WB/Ozon pattern. Update marketplace selectors so `yandex_market` appears as a real option anywhere the current UI offers source/target marketplace choice. In `app/static/app.js`, add element bindings for the new form and status nodes, extend `renderConnections()` to display Yandex Market configuration state, add submit handling that sends `PUT /api/v1/connections/yandex_market` with JSON payload `{ marketplace: "yandex_market", token, business_id: Number(...), campaign_id: Number(...) }`, and generalize selector sync logic so it no longer assumes only `wb` and `ozon`. Update `app/static/app.css` only as needed so the new form fits the existing layout without broken spacing.</action>
  <verify>python -m pytest tests/test_auth_and_connections.py -k yandex_market_connection</verify>
  <acceptance_criteria>
    - `app/static/index.html` contains `yandex_market`
    - `app/static/index.html` contains `business_id`
    - `app/static/index.html` contains `campaign_id`
    - `app/static/app.js` contains `connections/yandex_market`
    - `app/static/app.js` contains `yandex_market`
  </acceptance_criteria>
  <done>The built-in UI exposes Yandex Market connection management and marketplace selection.</done>
</task>

<task type="auto">
  <name>Task 2: Connect Yandex Market to source catalog rendering path</name>
  <files>app/static/app.js, app/static/index.html, app/static/app.css</files>
  <read_first>app/static/app.js, app/static/index.html, app/api/routes/catalog.py, app/schemas.py</read_first>
  <action>Update `app/static/app.js` so `loadProducts()`, product detail modal flow, and related catalog summary/rendering code work when `elements.sourceMarketplace.value === "yandex_market"`. Do not add preview/import behavior in this plan. Ensure source catalog interactions call the existing endpoints with `marketplace=yandex_market`, preserve product image rendering, and keep the details modal robust when Yandex Market fields differ from WB/Ozon raw payloads. If any explanatory text hardcodes WB-specific assumptions, replace it with marketplace-neutral wording so Yandex Market source catalog use is not mislabeled.</action>
  <verify>python -m pytest tests/test_auth_and_connections.py tests/test_yandex_market_client.py</verify>
  <acceptance_criteria>
    - `app/static/app.js` contains `marketplace=yandex_market` or logic equivalent for source catalog loading
    - `app/static/app.js` no longer contains selector fallback logic that only switches between `"wb"` and `"ozon"`
    - `app/static/app.js` still contains `openProductModal`
    - `app/static/app.js` still contains `loadProducts()`
  </acceptance_criteria>
  <done>The source catalog UI path can visibly operate with Yandex Market data without introducing preview/import claims.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest` succeeds
- [ ] Yandex Market appears in UI markup and JS handlers
- [ ] Selector logic remains valid for all marketplaces
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Yandex Market is visible and usable in the foundation-phase UI surface
</success_criteria>

<output>
After completion, create `.planning/phases/01-yandex-market-foundation/01-03-SUMMARY.md`
</output>
