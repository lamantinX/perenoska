---
phase: 01-yandex-market-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/clients/yandex_market.py
  - app/config.py
  - .env.example
  - app/services/container.py
  - tests/test_yandex_market_client.py
autonomous: true
requirements:
  - CAT-01
  - CAT-02
must_haves:
  truths:
    - "User can load Yandex Market products as a source marketplace through the existing catalog API"
    - "User can open Yandex Market product details normalized into the shared product schema"
    - "User can load Yandex Market categories through the existing catalog API"
  artifacts:
    - path: "app/clients/yandex_market.py"
      provides: "Yandex Market catalog client"
      min_lines: 80
      contains: "class YandexMarketClient"
    - path: "app/config.py"
      provides: "Yandex Market base URL configuration"
      contains: "YANDEX_MARKET"
    - path: "tests/test_yandex_market_client.py"
      provides: "Catalog client coverage for products, details, and categories"
      contains: "YandexMarketClient"
  key_links:
    - from: "app/services/container.py"
      to: "app/clients/yandex_market.py"
      via: "client factory branch for Yandex Market"
      pattern: "YandexMarketClient"
user_setup: []
---

<objective>
Add Yandex Market source catalog support with preview-ready core normalization.

Purpose: Phase 1 requires real source catalog value, not just marketplace labels. The backend must be able to list products, read details, and list categories for Yandex Market.
Output: New Yandex Market client, service wiring, and tests for products/details/categories.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-yandex-market-foundation/01-RESEARCH.md
@app/clients/base.py
@app/clients/ozon.py
@app/clients/wb.py
@app/services/catalog.py
@app/api/routes/catalog.py
@app/services/container.py
@tests/test_ozon_client.py
@tests/test_auth_and_connections.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Yandex Market client with campaign-scoped product and category reads</name>
  <files>app/clients/yandex_market.py, app/config.py, .env.example, app/services/container.py</files>
  <read_first>app/clients/base.py, app/clients/ozon.py, app/clients/wb.py, app/config.py, .env.example, app/services/container.py, .planning/phases/01-yandex-market-foundation/01-RESEARCH.md</read_first>
  <action>Add `YANDEX_MARKET_BASE_URL` support to `app/config.py` and `.env.example`, defaulting to the chosen official Partner API base URL. Create `app/clients/yandex_market.py` with `class YandexMarketClient(MarketplaceClient)` and constructor `(base_url: str, timeout: float)`. Use `httpx.AsyncClient`, Authorization header from `credentials["token"]`, and campaign/business values from encrypted credentials. Implement `list_products()` with the researched campaign-scoped source offer endpoint and normalize results to `ProductSummary` with `id`, `offer_id`, `title`, `description`, `category_id`, `category_name`, `price`, `stock`, `images`, and `raw`. Implement `get_product_details()` to return `ProductDetails` with preview-ready core fields plus `raw_sources`. Implement `list_categories()` from the Market category tree endpoint. For `get_category_attributes()`, `create_products()`, and `get_import_status()`, raise explicit `MarketplaceAPIError` messages stating that these capabilities are not part of Phase 1 yet. Register the new client in `MarketplaceClientFactory` under `Marketplace.YANDEX_MARKET` using `self.settings.yandex_market_base_url` and `self.settings.http_timeout_seconds`.</action>
  <verify>python -m pytest tests/test_yandex_market_client.py</verify>
  <acceptance_criteria>
    - `app/clients/yandex_market.py` contains `class YandexMarketClient`
    - `app/config.py` contains `yandex_market_base_url`
    - `.env.example` contains `YANDEX_MARKET_BASE_URL=`
    - `app/clients/yandex_market.py` contains `async def list_products`
    - `app/clients/yandex_market.py` contains `async def get_product_details`
    - `app/clients/yandex_market.py` contains `async def list_categories`
    - `app/services/container.py` contains `YandexMarketClient`
  </acceptance_criteria>
  <done>The project has a concrete Yandex Market client capable of source product and category reads.</done>
</task>

<task type="auto">
  <name>Task 2: Add client and API coverage for Yandex Market catalog endpoints</name>
  <files>tests/test_yandex_market_client.py</files>
  <read_first>tests/test_ozon_client.py, tests/test_transfer_preview.py, app/clients/yandex_market.py, app/api/routes/catalog.py</read_first>
  <action>Create `tests/test_yandex_market_client.py` with stubbed `_request()`-based tests covering `list_products()`, `get_product_details()`, and `list_categories()` for `YandexMarketClient`. Include assertions for request shapes using token + business_id + campaign_id context, and for normalized outputs containing `id`, `offer_id`, `title`, `category_id`, `category_name`, and `images`. Add one integration-style test in the same file or a helper-backed test that exercises the catalog routes through a client override and verifies `/api/v1/catalog/products`, `/api/v1/catalog/products/{id}`, and `/api/v1/catalog/categories` behave for `marketplace=yandex_market`.</action>
  <verify>python -m pytest tests/test_yandex_market_client.py</verify>
  <acceptance_criteria>
    - `tests/test_yandex_market_client.py` exists
    - `tests/test_yandex_market_client.py` contains `test_list_products`
    - `tests/test_yandex_market_client.py` contains `test_get_product_details`
    - `tests/test_yandex_market_client.py` contains `test_list_categories`
    - `tests/test_yandex_market_client.py` contains `marketplace=yandex_market`
  </acceptance_criteria>
  <done>Client-level and API-level tests prove Yandex Market source catalog support works through the existing backend surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_yandex_market_client.py` passes
- [ ] Catalog endpoints return normalized Yandex Market product and category data
- [ ] Existing WB/Ozon catalog tests remain intact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Yandex Market source catalog data is available through the current API shape
</success_criteria>

<output>
After completion, create `.planning/phases/01-yandex-market-foundation/01-02-SUMMARY.md`
</output>
