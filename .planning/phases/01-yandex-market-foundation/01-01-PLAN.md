---
phase: 01-yandex-market-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas.py
  - app/services/connections.py
  - app/api/routes/connections.py
  - tests/test_auth_and_connections.py
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
user_setup: []
must_haves:
  truths:
    - "User can save a Yandex Market connection with token, business_id, and campaign_id"
    - "User can list connections and see Yandex Market configuration state alongside WB and Ozon"
    - "Saved Yandex Market credentials are reusable by the service layer"
  artifacts:
    - path: "app/schemas.py"
      provides: "Marketplace enum and Yandex Market connection schema fields"
      contains: "YANDEX_MARKET"
    - path: "app/services/connections.py"
      provides: "Yandex Market credential encryption/decryption and masked connection responses"
      contains: "business_id"
    - path: "tests/test_auth_and_connections.py"
      provides: "Regression and Yandex Market connection coverage"
      contains: "yandex"
  key_links:
    - from: "app/api/routes/connections.py"
      to: "app/services/connections.py"
      via: "upsert_connection route delegates Yandex Market payload"
      pattern: "upsert_connection"
    - from: "app/services/connections.py"
      to: "app/schemas.py"
      via: "ConnectionUpsert validation and response typing"
      pattern: "ConnectionUpsert|ConnectionResponse"
---

<objective>
Establish Yandex Market as a valid marketplace in the backend connection contract.

Purpose: Phase 1 cannot proceed without a stable marketplace enum, validated credential payload, encrypted persistence, and reusable service/container wiring.
Output: Backend connection support for Yandex Market with tests proving save/list/reuse behavior.
</objective>

<execution_context>
@C:/Users/zelen/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/zelen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-yandex-market-foundation/01-RESEARCH.md
@app/schemas.py
@app/services/connections.py
@app/services/container.py
@app/api/routes/connections.py
@tests/test_auth_and_connections.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend marketplace and connection schemas for Yandex Market</name>
  <files>app/schemas.py, app/services/connections.py, app/api/routes/connections.py</files>
  <read_first>app/schemas.py, app/services/connections.py, app/api/routes/connections.py, app/services/catalog.py</read_first>
  <action>Add `Marketplace.YANDEX_MARKET = "yandex_market"` in `app/schemas.py`. Extend `ConnectionUpsert` with `business_id: int | None` and `campaign_id: int | None`. Update the post-validation method so Yandex Market requires all three values: `token`, `business_id`, and `campaign_id`, with a dedicated validation error message. Update `ConnectionService.upsert_connection()` so the encrypted credential payload for Yandex Market is exactly `{"token": payload.token, "business_id": payload.business_id, "campaign_id": payload.campaign_id}`. Keep existing WB and Ozon branches unchanged. Ensure `list_connections()` returns a Yandex Market entry in the same style as existing marketplaces and that `masked_fields` includes masked `token` plus stringified `business_id` and `campaign_id` values.</action>
  <verify>python -m pytest tests/test_auth_and_connections.py -k yandex_market_connection</verify>
  <acceptance_criteria>
    - `app/schemas.py` contains `YANDEX_MARKET = "yandex_market"`
    - `app/schemas.py` contains `business_id: int | None = None`
    - `app/schemas.py` contains `campaign_id: int | None = None`
    - `app/services/connections.py` contains `"business_id": payload.business_id`
    - `app/services/connections.py` contains `"campaign_id": payload.campaign_id`
  </acceptance_criteria>
  <done>Yandex Market is a valid marketplace in connection schemas and service-level credential persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Yandex Market client path and add connection regression tests</name>
  <files>tests/test_auth_and_connections.py</files>
  <read_first>tests/test_auth_and_connections.py, app/main.py, app/api/routes/connections.py, app/services/connections.py</read_first>
  <action>Add tests in `tests/test_auth_and_connections.py` covering: register/login, `PUT /api/v1/connections/yandex_market` with `token`, `business_id`, and `campaign_id`, and `GET /api/v1/connections` returning an entry where `marketplace == "yandex_market"` and `is_configured == true`. Assert that masked fields include the stored token in masked form and expose stringified `business_id` and `campaign_id`. Keep the existing WB/Ozon assertions green and explicit.</action>
  <verify>python -m pytest tests/test_auth_and_connections.py</verify>
  <acceptance_criteria>
    - `tests/test_auth_and_connections.py` contains `"/api/v1/connections/yandex_market"`
    - `tests/test_auth_and_connections.py` contains `"business_id"`
    - `tests/test_auth_and_connections.py` contains `"campaign_id"`
  </acceptance_criteria>
  <done>Container wiring and regression tests prove the Yandex Market connection flow works alongside WB and Ozon.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_auth_and_connections.py` passes
- [ ] No existing marketplace connection flow is broken
- [ ] Yandex Market connection payload is encrypted and reusable through service calls
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend accepts Yandex Market as a first-class connection target
</success_criteria>

<output>
After completion, create `.planning/phases/01-yandex-market-foundation/01-01-SUMMARY.md`
</output>
