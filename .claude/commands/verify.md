Запусти полную проверку проекта:
1. `python -m pytest` — все тесты
2. `powershell -ExecutionPolicy Bypass -File scripts/verify.ps1` — скрипт верификации
3. `git status --short --branch` — статус репозитория

Сообщи итог каждого шага. Если что-то упало — покажи детали.
