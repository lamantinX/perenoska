Покажи текущее состояние проекта perenoska:

1. **Git статус** — `git status --short --branch` и последние 5 коммитов
2. **Тесты** — быстрый прогон `python -m pytest --tb=no -q`
3. **Роадмап** — прочитай `.planning/STATE.md` и `.planning/ROADMAP.md`, резюмируй что сделано и что следующее
4. **Открытые задачи** — есть ли TODO/FIXME в коде (`grep -r "TODO\|FIXME" app/ --include="*.py"`)

Выведи краткое резюме состояния проекта.
