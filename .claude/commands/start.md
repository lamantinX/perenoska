Запусти сервер разработки: `uvicorn app.main:app --reload`

Перед запуском проверь:
1. Существует ли файл `.env` (если нет — скопировать из `.env.example` и предупредить)
2. Установлены ли зависимости (наличие `.venv/`)

Сервер будет доступен на http://localhost:8000
